# Project scripts

This directory is reserved for repeatable, non-secret project commands. Normal
Rust build, format, check, and test operations use Cargo directly; no shell
script is required by the repository skeleton.
